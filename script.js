const Player = (name, sign) => {
    const getName = () => name;
    const getSign = () => sign;

    return {getName, getSign};
}

const gameBoard = (function() {
   const board = ["", "", "", "", "", "", "", "", ""];

   
})();